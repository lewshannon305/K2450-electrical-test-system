# Keithley 2450 电学测试系统

面向 Keithley 2450 SourceMeter 的桌面电学测试平台。程序使用 PyQt6 构建统一界面，通过 PyVISA 控制一台或两台 2450，集成七种测量流程、实时显示、自动保存、结果元数据和论文级绘图。

> 本项目会直接控制源表输出。首次连接样品前，请先使用限流、低电压和空载条件验证接线、量程、端子与紧急停止流程。

## 最简单的使用方法

在 GitHub 页面点击绿色 **Code** 按钮，再点击 **Download ZIP**。解压完整压缩包后，双击根目录的 `K2450电学测试系统.exe` 即可启动，不需要安装 Python，也不需要创建虚拟环境。

请保留 `runtime` 文件夹并让它与 EXE 位于同一目录；EXE 会调用其中的 Python 运行环境和第三方库。`configs` 是可查看、可编辑的配置文件，`source` 是完整 Python 源码。

## 主要功能

- 七种测量模块统一入口，支持单台或双台 Keithley 2450
- 自动扫描 VISA 资源，并校验仪器型号、端子、量程和关键参数
- 全局单任务保护，避免多个模块同时占用同一台仪器
- 正常停止、强制终止、退出拦截和测量结束自动归零
- 原始数据、运行状态与 `_meta.json` 元数据分开保存
- 支持完整结果及用户停止后的有效部分结果
- 自动生成 SVG、PDF、PNG，提供独立的绘图设置和预览界面
- 保存或加载全部模块参数、波形、开关状态与绘图方案
- 内置自动化测试，覆盖硬件安全、采集完整性、配置兼容和绘图逻辑

## 运行环境

推荐环境：

- Windows 10/11
- Python 3.10 或更高版本
- Keithley 2450 SourceMeter
- NI-VISA、Keithley I/O Layer 或其他可被 PyVISA 识别的 VISA 后端
- GPIB、USB-TMC 或局域网 VISA 连接

程序会通过 `*IDN?` 检查仪器，当前仅接受 Keithley 2450。双表测试需要两台地址不同的 2450。

## 从源码运行

### 1. 获取代码

```powershell
git clone https://github.com/lewshannon305/K2450-electrical-test-system.git
cd K2450-electrical-test-system
```

### 2. 创建虚拟环境

```powershell
py -m venv .venv
.\.venv\Scripts\Activate.ps1
python -m pip install --upgrade pip
```

### 3. 安装依赖

```powershell
pip install -r source/requirements.txt
```

主要 Python 依赖包括 PyQt6、PyQtGraph、PyVISA、NumPy、SciPy、Matplotlib 和 Markdown，具体最低版本以 `source/requirements.txt` 为准。

### 4. 检查 VISA 连接

源表开机并连接电脑后，可在 PowerShell 中先检查资源：

```powershell
python -c "import pyvisa; print(pyvisa.ResourceManager().list_resources())"
```

若返回空元组或报找不到 VISA library，请先安装或修复 VISA 后端，再启动程序。

### 5. 启动

```powershell
python source/main.py
```

## 第一次测量

1. 进入需要的测试模块，点击“扫描设备”。
2. 选择正确的 VISA 地址，以及 `FRONT` 或 `REAR` 端子。
3. 检查电压范围、电流限制、固定量程、NPLC、步长和稳定时间。
4. 选择数据目录和文件名前缀。
5. 先用安全的小电压进行短测试，确认接线和数据列正确。
6. 点击“开始”执行测量；需要中止时优先使用“停止”。
7. 仅在异常或紧急情况下使用“强制终止”，随后确认仪器面板输出已经关闭。

## 七种测试模块

| 模块 | 仪器 | 用途与当前能力 |
| --- | --- | --- |
| 断裂结 | 1 台 | 自适应偏压步进和电导反馈，记录电压、电流及以 $G_0$ 表示的电导 |
| 循环 IV 特性扫描 | 1–2 台 | 单向、双向及迟滞 IV；可选栅压表，并支持单值、升降序步进和自定义栅压序列 |
| 栅压特性扫描 | 2 台 | 固定源漏偏压下执行四段栅压扫描，同时记录 $I_{sd}$ 与 $I_g$ |
| 二维 Mapping 扫描 | 2 台 | 栅压与偏压二维扫描，包含预扫描、完整扫描、平滑和微分电导绘图 |
| It 特性扫描 | 1–2 台 | 固定或阶梯偏压、可选固定或阶梯栅压，使用 2450 内部触发模型连续采集 |
| 任意偏压波形测试 | 1–2 台 | 在波形编辑器中定义多段偏压与持续时间，可选固定栅压 |
| 任意栅压波形测试 | 2 台 | 在固定偏压下执行多段自定义栅压波形，同时记录 $I_{sd}$ 与 $I_g$ |

双电平或四段开关测试直接在任意波形编辑器中定义。例如依次加入测试电平、状态 1、测试电平、状态 2，并设置各段持续时间和循环次数，即可完成原有的开关序列；无需单独的开关测试模块。

### It 高速采集说明

It 模块不是逐点发送 `READ?`，而是使用 2450 内部触发模型把连续读数写入仪器缓冲区，采集完成后再批量传输。这样可以降低 GPIB 查询对时间轴的扰动。

- `points` 模式按点数采集，`duration` 模式按时长估算点数。
- 原始电流始终保留，不会因为后处理被覆盖。
- 自动调零可设为每个数据块执行一次或关闭。
- 工频谐波拟合默认关闭；启用后只增加辅助处理列及诊断信息。
- 实际采样率、是否截断和处理参数写入对应元数据文件。

Keithley 2450 不具备专用 digitize 功能。采样速度和噪声取决于 NPLC、固定量程、自动调零、总线与固件状态，应以每次测量的元数据为准。

## 数据与元数据

各模块将数据保存为制表符分隔的文本文件，可用 Origin、Excel、Python 或 MATLAB 读取。程序使用唯一文件名和原子写入，已有文件不会被静默覆盖。

典型输出结构如下：

```text
选择的数据目录/
├── 测量结果.txt
├── metadata/
│   └── 测量结果_meta.json
└── plots/
    ├── 测量结果.svg
    ├── 测量结果.pdf
    └── 测量结果.png
```

元数据通常包含：

- 模块名称、保存时间和结果状态
- 完整、部分完成或错误状态，以及中止原因
- 仪器标识、端子和关键采集参数
- 栅压序列、实际采样率或滤波诊断等模块专用信息

若停止时已经获得有效数据，程序会尽力保存部分结果；对应图文件带有 `PARTIAL` 标记。发生安全告警或保存错误时，请同时查看界面日志和 `_meta.json`，不要只根据曲线判断测量是否完整。

## 自动绘图

“绘图”菜单提供以下操作：

- “测试结束后自动绘图”：控制测量结束后的后台绘图
- “绘图设置”：修改通用设置及七个模块的独立设置
- “绘制最近一次测试”：使用当前方案重新绘制最近结果
- “查看最近绘图”：预览最近生成的 PNG
- “打开最近绘图目录”：打开结果所在目录

默认正式输出包含可编辑文字的 SVG、嵌入字体的 PDF 和 PNG。普通模块默认 300 DPI，Mapping 默认 450 DPI。各模块可独立设置：

- Nature 单栏 89 mm、1.5 栏 120 mm 或双栏 183 mm 尺寸
- 字体、字号、线宽、颜色、图例、网格、刻度和坐标范围
- 线性或对数坐标、电流单位自动换算
- IV 文件/栅压/循环分组方式
- 栅压特性叠加或拼图方式
- Mapping 平滑参数、微分电导与逐条 IV 输出
- It 统计直方图、均值、中位数和标准差

自动绘图只读取本次测量成功记录的文件清单，不会把同目录中的旧数据混入新图。

## 配置文件

“文件 → 保存配置”会导出版本 2 JSON，包含：

- 七个模块的输入参数
- 复选框和单选按钮状态
- 数据目录
- 任意波形定义
- IV 栅压序列设置
- 当前绘图方案

“文件 → 加载配置”支持当前版本 2 配置，也兼容未标版本的旧配置。

仓库提供三个实验配置样例：

- `configs/default.json`
- `configs/5T.json`
- `configs/9T.json`

这些文件保留实验参数和 VISA 地址，但不包含特定电脑的绝对数据目录。换电脑、换接线或换仪器地址后，必须先在界面中重新扫描和确认，不能直接照搬。

绘图界面中的“设为默认”会在本机生成 `configs/plotting_default.json`，供以后启动时自动加载。

## 安全机制

- 同一时间只允许一个测量模块运行。
- 启动前验证仪器型号、地址冲突、端子、量程、限流、NPLC 和步进计划。
- 慢速扫描保持连续自动调零；时间敏感测量可在数据块前单次调零。
- 正常停止会结束采集并执行安全归零。
- 强制终止会请求紧急停止、快速归零并确认输出关闭。
- 测量仍在运行时关闭主窗口，程序会先请求停止并等待硬件清理。
- 归零、关闭输出或 VISA 清理失败会保留为告警，不会显示成成功完成。

软件保护不能替代硬件保护。请根据器件承受能力设置源表电流限制，并在高风险测试中使用外部限流、联锁或人工监护。

## 项目结构

```text
K2450-electrical-test-system/
├── K2450电学测试系统.exe       # 普通用户双击这里
├── runtime/                    # EXE 需要的运行环境和依赖，不要删除
├── configs/                    # 平铺的默认、5T 和 9T 配置
├── source/                     # 完整可编辑源码
│   ├── main.py                 # 主窗口、导航、配置和绘图入口
│   ├── requirements.txt        # Python 运行依赖
│   ├── core/                   # 公共逻辑、硬件安全与绘图
│   ├── modules/                # 七个测量模块
│   ├── tests/                  # 自动化测试
│   └── packaging/              # Windows 打包脚本
└── Readme.md
```

这个结构让普通用户下载整个仓库后直接运行，也让开发者在同一份仓库里查看和修改 Python 源码。更新程序时，EXE 与 `runtime` 必须作为一套一起更新。

## 运行测试

测试使用模拟仪器与临时文件，不需要连接真实 2450：

```powershell
python -m unittest discover -s source/tests -t source -v
```

## 重新生成 Windows 版本

开发电脑先安装源码依赖和 PyInstaller，然后从仓库根目录运行：

```powershell
python -m pip install -r source/requirements.txt
python -m pip install pyinstaller
powershell -ExecutionPolicy Bypass -File source/packaging/build_windows.ps1
```

脚本会先运行测试，再将最新的 `K2450电学测试系统.exe` 和 `runtime` 更新到仓库根目录。提交并推送这两项后，GitHub 的 **Download ZIP** 就会同时包含最新源码和最新可运行程序。

## 常见问题

### 扫描不到仪器

1. 确认 VISA 后端和 GPIB/USB/LAN 驱动已安装。
2. 用厂商工具确认仪器可以被发现。
3. 运行前面的 PyVISA 资源检查命令。
4. 检查地址是否与源表面板一致，以及是否被其他程序占用。

### 提示“仅支持 Keithley 2450”

程序会检查 `*IDN?` 返回值。请确认选中的资源确实是 2450，而不是总线上的其他仪器。

### FRONT/REAR 或量程校验失败

程序配置后会读取仪器实际状态。请检查面板设置、固件状态和接线，避免在未查明原因时绕过校验。

### 绘图没有生成

确认该模块绘图开关已启用、数据文件保存成功，并查看界面日志。部分结果只有在“绘制部分结果”启用时才会生成图。

## 日常更新代码

在没有未提交本地修改时执行：

```powershell
git pull --ff-only
```

修改程序后提交并推送：

```powershell
git add .
git commit -m "说明本次修改"
git push
```

## 免责声明

本项目用于实验室研究和自动化测量。使用者应自行核对接线、量程、限流、样品耐受范围和紧急处置流程。作者不对错误参数、接线故障、通信异常或无人值守运行造成的仪器与样品损失负责。
